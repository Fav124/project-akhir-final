<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Deisa"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Login (Launcher) -->
        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".RegisterActivity" android:exported="false" />

        <!-- Main Dashboard -->
        <activity android:name=".MainActivity" android:exported="false" />

        <!-- Sakit Feature -->
        <activity android:name=".SakitListActivity" android:exported="false" />
        <activity android:name=".DetailSakitActivity" android:exported="false" />
        <activity android:name=".AddEditSakitActivity" android:exported="false" />

        <!-- Santri Feature -->
        <activity android:name=".SantriListActivity" android:exported="false" />
        <activity android:name=".SantriDetailActivity" android:exported="false" />

        <!-- Kelas Feature -->
        <activity android:name=".KelasListActivity" android:exported="false" />
        <activity android:name=".AddEditKelasActivity" android:exported="false" />

        <!-- Jurusan Feature -->
        <activity android:name=".JurusanListActivity" android:exported="false" />
        <activity android:name=".AddEditJurusanActivity" android:exported="false" />

        <!-- Diagnosis Feature -->
        <activity android:name=".DiagnosisListActivity" android:exported="false" />
        <activity android:name=".AddEditDiagnosisActivity" android:exported="false" />
        
        <!-- Profile -->
        <activity android:name=".ProfileActivity" android:exported="false" />
        
        <!-- History -->
        <activity android:name=".HistoryActivity" android:exported="false" />

        <!-- Obat Feature -->
        <activity android:name=".ObatListActivity" android:exported="false" />

        <!-- Laporan Feature -->
        <activity android:name=".LaporanActivity" android:exported="false" />

    </application>

</manifest>